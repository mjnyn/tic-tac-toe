{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "description": "A simple Tic Tac Toe game with tests and CI/CD",
  "main": "index.html",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "build": "vite build",
    "start": "vite --host"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=20.14.0"
  }
}